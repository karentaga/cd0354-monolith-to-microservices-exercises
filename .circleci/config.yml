# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/reference/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/guides/orchestrate/jobs-steps/#jobs-overview & https://circleci.com/docs/reference/configuration-reference/#jobs
jobs:
  say-hello:
    # Specify the execution environment. You can specify an image from Docker Hub or use one of our convenience images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/guides/execution-managed/executor-intro/ & https://circleci.com/docs/reference/configuration-reference/#executor-job
    docker:
      # Specify the version you desire here
      # See: https://circleci.com/developer/images/image/cimg/base
      - image: cimg/base:current
    working_directory: ~/project
    # Add steps to the job
    # See: https://circleci.com/docs/guides/orchestrate/jobs-steps/#steps-overview & https://circleci.com/docs/reference/configuration-reference/#steps
    steps:
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
      # 1️⃣ Checkout the repo
      - checkout

      # 2️⃣ Ensure we are on the correct branch
      - run:
          name: Checkout circleci-project-setup branch
          command: |
            git fetch origin circleci-project-setup
            git checkout circleci-project-setup

      # 3️⃣ Debug: cd, verify Dockerfile is present
      - run:
          name: Verify Dockerfile
          command: |
            cd lesson-6-kubernetes-for-production/exercises/simple-express

            pwd
            ls -la
            ls -la Dockerfile

      # 4️⃣ Setup Docker engine
      - setup_remote_docker
      # 5️⃣ Build and push Docker image
      - run:
          name: Build and push Docker image
          command: |
            cd lesson-6-kubernetes-for-production/exercises/simple-express

            # Build the image
            docker build -t karenht/simple-express-app:1.0.0 .
            
            # Log in to Docker Hub
            echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
            
            # Push the image
            docker push karenht/simple-express-app:1.0.0        

# Orchestrate jobs using workflows
# See: https://circleci.com/docs/guides/orchestrate/workflows/ & https://circleci.com/docs/reference/configuration-reference/#workflows
workflows:
  say-hello-workflow: # This is the name of the workflow, feel free to change it to better match your workflow.
    # Inside the workflow, you define the jobs you want to run.
    jobs:
      - say-hello